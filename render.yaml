services:
  - type: web
    name: vm-mobile
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
      - key: VM_CLOUD
        value: "1"
